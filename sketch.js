// establish colour variables
let startCol, tarCol;
let startx, starty;
let divisions

function setup() {
    createCanvas(500, 500);
    colorMode(HSB)
    startCol = color(100, 100, 200)
    tarCol = color(200, 100, 100)
    startx = 
    starty = 

}

function draw() {
    background(220);
}